CREATE TABLE [List].[BillingInterval] (
    [BillingIntervalID] INT          NOT NULL,
    [Name]              VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_BillingInterval] PRIMARY KEY CLUSTERED ([BillingIntervalID] ASC)
);

