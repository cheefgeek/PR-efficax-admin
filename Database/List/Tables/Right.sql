CREATE TABLE [List].[Right] (
    [RightID]     INT           NOT NULL,
    [Name]        VARCHAR (50)  NOT NULL,
    [Description] VARCHAR (200) NOT NULL,
    CONSTRAINT [PK_Right] PRIMARY KEY CLUSTERED ([RightID] ASC)
);

